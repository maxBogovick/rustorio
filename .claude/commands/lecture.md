---
description: Написать лекцию курса rustorio по мастер-промпту v2 (PROMPT.md)
---

Напиши лекцию $ARGUMENTS для курса rustorio.

Открой `docs/lectures/PROMPT.md` и выполни его ЦЕЛИКОМ как рабочую инструкцию —
от раздела «Контекст» и этапа 0 до Definition of Done, включая чек-лист
TEMPLATE §5. Номер и тему возьми из аргумента выше; если указан только номер —
тему и срез возьми из строки этой лекции в `docs/lectures/COURSE.md`.

Три правила, которые чаще всего теряются (они уже есть в промпте — это дубль
для надёжности):

1. Срез лекции покажи ПЕРВЫМ сообщением (этап 1) — с целями, заблуждениями и
   бюджетами новизны/объёма — и продолжай не останавливаясь, если нет настоящей
   развилки.
2. Java-код НЕ пишется с нуля: он берётся из эталона
   `/Users/maxim/Projects/Java/rustorio` 1:1 и переносится прямо в исходники
   `rustorio_lesson` по мере написания шагов. НИЧЕГО не коммитить.
3. Эксперименты откатывай точечной обратной правкой — никогда
   `git restore` / `git checkout`: в дереве нескоммиченный код лекции.
