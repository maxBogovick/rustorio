# examplemod — L3 acceptance fixture

Not the starter template. Used by engine tests to prove a jar can add a custom
`TransportNode`. For new mods start at [`external-mod-template`](../external-mod-template/)
or `./gradlew initMod` — see [`docs/start-here.md`](../../docs/start-here.md).
