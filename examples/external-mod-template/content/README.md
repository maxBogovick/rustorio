# Optional L0 JSON

This template registers content from Java (`TemplateMod`). You can still drop
`items/`, `recipes/`, `buildings/`, `maps/`, `techs/` JSON here — see engine
`docs/first-mod.md` and `docs/schemas/`.
