package com.rustorio;

/**
 * Как здание выглядит ЛОГИЧЕСКИ — имя картинки, а не сама картинка.
 *
 * <p>Это описание живёт в домене и НЕ знает ни про libGDX, ни про текстуры: здесь лишь «я
 * выгляжу как горячая печь». Перевести имя в конкретный спрайт — забота графики (она и держит
 * пиксели). Так домен рассказывает, КАК он выглядит, не завися от того, ЧЕМ его рисуют.
 */
public enum Sprite {
    MINER,
    CHEST,
    FURNACE_HOT,
    FURNACE_COLD,
    BELT_EMPTY,
    BELT_FULL,
    SPLITTER,
    UNDERGROUND_IN,
    UNDERGROUND_OUT,
    LAB
}
