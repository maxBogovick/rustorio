/**
 * Fluent registration helpers for code mods (API v2 phase 1) — {@link
 * com.rustorio.api.dsl.ContentDsl} and friends. Built on top of {@link
 * com.rustorio.api.mod.RegistrationContext}; does not replace JSON {@code content/} loaders or the
 * existing {@code register}/{@code peek} registries.
 */
@NullMarked
package com.rustorio.api.dsl;

import org.jspecify.annotations.NullMarked;
