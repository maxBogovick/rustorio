package com.rustorio.core;

/**
 * Всё, что зданию нужно знать про текущий шаг симуляции (тик).
 *
 * <p><b>Зачем «коробка» вместо простого {@code float dt}.</b> Сегодня зданиям нужна
 * только длительность тика. Но стоит зданиям понадобиться что-то ещё общее — и с
 * «голым» параметром пришлось бы править сигнатуру {@code update} во ВСЕХ зданиях
 * сразу. С коробкой сигнатура не меняется никогда: нужно новое общее данное —
 * добавляется поле СЮДА. (В L13 так приедет баланс апгрейдов.)
 *
 * <p><b>Один объект на тик, а не на здание.</b> Все поля одинаковы для всех зданий в
 * пределах шага, поэтому контекст создаётся один раз за тик, а не на каждое здание.
 */
public record TickContext(float dt) {
}
