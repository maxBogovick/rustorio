package com.rustorio.model;

import com.rustorio.core.Item;

/**
 * Где нарисовать предмет, едущий по ленте: непрерывные координаты поля.
 *
 * <p>Целое значение {@code x}/{@code y} — это ЦЕНТР соответствующей клетки,
 * дробное — точка между клетками. Это по-прежнему координаты ДОМЕНА (клетки), а
 * не пиксели: перевод в пиксели — забота слоя отрисовки, домен про экран ничего
 * не знает.
 *
 * <p>Именно здесь появляется дробь, которой принципиально нет в симуляции
 * (см. {@link BeltItem}): модель считает целыми слотами, а красота — в рендере.
 */
public record BeltItemPos(Item item, float x, float y) {
}
