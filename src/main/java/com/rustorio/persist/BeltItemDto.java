package com.rustorio.persist;

import com.rustorio.core.Item;

/**
 * Один предмет, едущий по ленте: на какой клетке и в каком слоте внутри неё он стоит.
 *
 * <p>Почему координата клетки, а не «глобальный слот линии»: линия — производная сущность,
 * её при загрузке пересобирает {@link com.rustorio.model.BeltNetwork} из расставленных лент,
 * и никакого «слота 7 от начала линии» до сборки ещё не существует. А клетка и позиция в ней
 * (0..slots-1) — абсолютны и переживают пересборку. По ним загрузчик и вернёт предмет на
 * место (см. {@code World.restoreBeltItem}).
 */
public record BeltItemDto(int x, int y, int slot, Item item) {
}
